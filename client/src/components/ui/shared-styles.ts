import styled from "styled-components";

export const SharedStyles = {
  PageContainer: styled("div")({
    width: "600px",
    maxWidth: "100%",
    display: "block",
    padding: "1rem",
    margin: "auto",
  }),
};
